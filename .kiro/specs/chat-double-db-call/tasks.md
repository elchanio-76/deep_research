# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Double DB Call on Valid Chat Request
  - **CRITICAL**: This test MUST FAIL on unfixed code — failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior — it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate `db_sessions.load_session` is called twice per valid `POST /chat` request
  - **Scoped PBT Approach**: Scope the property to the concrete failing case — a valid session row with `report_markdown` set — to ensure reproducibility
  - Create `tests/test_property_chat_double_db_call.py`
  - Mock `db_sessions.load_session` with `AsyncMock` returning a row dict with `report_markdown` set and mock `rm.load_session` to avoid real DB/LLM calls
  - Use `hypothesis` to generate varied session row dicts (varying `search_mode`, `cost_effective_search`, `report_markdown` content) and assert that `db_sessions.load_session` call count equals 1 for each
  - Run test on UNFIXED code: `python -m pytest tests/test_property_chat_double_db_call.py::test_property_bug_condition_single_db_call -x`
  - **EXPECTED OUTCOME**: Test FAILS with counterexample showing `db_sessions.load_session` called 2 times (confirms bug exists)
  - Document the counterexample found (e.g., `db_sessions.load_session` called twice for session with `report_markdown="# Report"`)
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Observable HTTP Behavior and ResearchManager Hydration Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe on UNFIXED code: `rm.load_session(session_id_str)` with a DB mock returning a row hydrates all six fields (`current_session_id`, `last_query`, `search_mode`, `cost_effective_search`, `session_usage`, `report`) correctly
  - Observe on UNFIXED code: missing session → HTTP 404 `"Session not found"`; session without `report_markdown` → HTTP 404 `"No report available for this session"`
  - Add to `tests/test_property_chat_double_db_call.py`:
    - **Property 2a**: For any valid session row dict (generated by hypothesis with varied `search_mode`, `cost_effective_search`, `usage_jsonb`, `report_markdown`), calling `rm.load_session(session_id_str)` with a DB mock returning that row sets all six hydration fields identically to calling it with `prefetched_row=row` (after fix — but write the assertion now against unfixed code to establish baseline)
    - **Property 2b**: For any session row dict, `rm.load_session` without `prefetched_row` still calls `db_sessions.load_session` exactly once (Gradio path preservation)
    - **Property 2c**: Missing session (`db_sessions.load_session` returns `None`) → HTTP 404 `"Session not found"` (requirement 3.1)
    - **Property 2d**: Session with `report_markdown=None` or `""` → HTTP 404 `"No report available for this session"` (requirement 3.2)
  - Run tests on UNFIXED code: `python -m pytest tests/test_property_chat_double_db_call.py -k "preservation" -x`
  - **EXPECTED OUTCOME**: All preservation tests PASS on unfixed code (confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. Fix double DB call in chat handler

  - [x] 3.1 Add `prefetched_row` parameter to `ResearchManager.load_session`
    - In `src/core/research_manager.py`, change the signature of `load_session` to:
      `async def load_session(self, session_id: str, prefetched_row: dict | None = None)`
    - Inside `load_session`, replace the unconditional `session_row = await db_sessions.load_session(self.pool, session_uuid)` with:
      ```python
      if prefetched_row is not None:
          session_row = prefetched_row
      else:
          session_row = await db_sessions.load_session(self.pool, session_uuid)
      ```
    - All six hydration assignments (`current_session_id`, `last_query`, `search_mode`, `cost_effective_search`, `session_usage`, `report`) remain unchanged — they read from `session_row` regardless of how it was obtained
    - Default `prefetched_row=None` preserves the Gradio thin-client call path (requirement 3.5)
    - _Bug_Condition: isBugCondition(request) where db_sessions.load_session returns non-None for request.session_id_
    - _Expected_Behavior: load_session uses prefetched_row directly when supplied, skipping the DB fetch_
    - _Preservation: Gradio path (prefetched_row=None) continues to call db_sessions.load_session exactly once_
    - _Requirements: 2.1, 2.2, 3.3, 3.5_

  - [x] 3.2 Pass prefetched row from `chat` handler to `rm.load_session`
    - In `src/api/chat.py`, replace:
      `await rm.load_session(str(body.session_id))`
      with:
      `await rm.load_session(str(body.session_id), prefetched_row=session)`
    - The `session` variable already holds the dict returned by the guard-check fetch; no new DB call is needed
    - The guard checks (`session is None`, `not session.get("report_markdown")`) remain in place and unchanged
    - _Bug_Condition: isBugCondition(request) — session row already fetched by guard check and discarded_
    - _Expected_Behavior: single db_sessions.load_session call per valid POST /chat request lifecycle_
    - _Preservation: HTTP 404 guards (requirements 3.1, 3.2) and SSE streaming (requirement 3.4) unchanged_
    - _Requirements: 2.1, 2.2, 3.1, 3.2, 3.4_

  - [x] 3.3 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Single DB Call on Valid Chat Request
    - **IMPORTANT**: Re-run the SAME test from task 1 — do NOT write a new test
    - The test from task 1 asserts `db_sessions.load_session` call count == 1; it encodes the expected behavior
    - Run: `python -m pytest tests/test_property_chat_double_db_call.py::test_property_bug_condition_single_db_call -x`
    - **EXPECTED OUTCOME**: Test PASSES (confirms the double-call bug is fixed)
    - _Requirements: 2.1, 2.2_

  - [x] 3.4 Verify preservation tests still pass
    - **Property 2: Preservation** - Observable HTTP Behavior and ResearchManager Hydration Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 — do NOT write new tests
    - Run: `python -m pytest tests/test_property_chat_double_db_call.py -k "preservation" -x`
    - **EXPECTED OUTCOME**: All preservation tests PASS (confirms no regressions in HTTP behavior, hydration state, or Gradio path)
    - Confirm requirements 3.1–3.5 are all satisfied

- [x] 4. Checkpoint — Ensure all tests pass
  - Run the full test suite: `python -m pytest`
  - Confirm all existing tests continue to pass (no regressions in unrelated modules)
  - Confirm both property tests pass: bug condition (Property 1) and preservation (Property 2)
  - Run linter: `ruff check src/api/chat.py src/core/research_manager.py tests/test_property_chat_double_db_call.py`
  - Ensure all tests pass; ask the user if questions arise
